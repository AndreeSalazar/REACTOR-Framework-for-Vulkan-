"""Python modules for REACTOR Bridge.

- `transport/` — Protocol types + WebSocket client (standalone, no Blender deps)
- `scripts/` — Utility scripts (packaging, etc.)
- `tests/` — Unit and integration tests
"""
